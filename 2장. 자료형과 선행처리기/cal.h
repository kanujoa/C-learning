#include <stdio.h>
#define ADD(x, y) ((x) + (y))
#define SUB(x, y) ((x) - (y))

// 헤더 파일의 확장자는 .h이어야 한다. 매크로 함수를 정의하였다.
// include할 헤더 파일(cal.h)을 #include cal.h로 작성할 것이라면 실행 파일(예제 2-3.c)은 같은 디렉토리에 존재해야 한다.